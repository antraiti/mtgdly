import { useEffect, useState } from 'react'
import './App.css'

/** TODO:
 * - load in card data
 * - pick random card and set to state
 * - add card search 
 * - template row for magic components
 */
function App() {
  useEffect(() => {
    console.log("test");
  }, []);

  return (
    <html data-theme="dark">
      <h3>mtgdly</h3>
    </html>
  )
}

export default App
